<form method="post" action="{{ path('app_posts_crud_delete', {'id': post.id}) }}" onsubmit="return confirm('¿Quieres eliminar este post?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post.id) }}">
    <button class="btn">Eliminar</button>
</form>
